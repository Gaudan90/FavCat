import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    touchable: {
      borderRadius: 25,
      overflow: 'hidden',
    },
    gradient: {
      padding: 15,
      minWidth: 200,
      alignItems: 'center',
    },
    text: {
      color: '#ffffff',
      fontSize: 18,
      fontWeight: '600',
    },
  });